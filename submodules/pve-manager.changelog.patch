diff --git a/debian/changelog b/debian/changelog
index 8bd3f05e..17494f7c 100644
--- a/debian/changelog
+++ b/debian/changelog
@@ -1,10 +1,16 @@
+pve-manager (9.2.5+wsh1) trixie; urgency=medium
+
+  * Refresh patches: pve-manager
+
+ -- hwinther <hwinther@users.noreply.github.com>  Fri, 31 Jul 2026 21:17:55 +0000
+
 pve-manager (9.2.5) trixie; urgency=medium
 
   * ceph: add guided rolling-restart for Ceph daemons, available both from the
     API and the web interface, so operators can safely cycle all OSDs,
     monitors, managers or metadata servers of a cluster after a configuration
     or kernel change without hand-rolled scripts. Each step waits for the
     previous daemon to rejoin and gates on Ceph's own 'ok-to-stop' check to
     keep data placement and quorum available, aborting if the cluster health
     degrades mid-run. OSD restarts are scoped per node and guarded by 'noout',
     while monitor, manager and metadata server restarts run cluster-wide. A
