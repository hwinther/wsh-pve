diff --git a/debian/changelog b/debian/changelog
index 63ded16d..5376e705 100644
--- a/debian/changelog
+++ b/debian/changelog
@@ -1,10 +1,16 @@
+qemu-server (9.1.15+wsh1) trixie; urgency=medium
+
+  * Refresh patches: qemu-server
+
+ -- hwinther <hwinther@users.noreply.github.com>  Sun, 24 May 2026 20:34:27 +0000
+
 qemu-server (9.1.15) trixie; urgency=medium
 
   * cpu models: only include custom CPU models matching the host architecture
     in the model listing, instead of also showing custom models with an
     incompatible architecture (e.g. aarch64 on an x86_64 host).
 
   * cpu config: write the default 'reported-model' (kvm64) explicitly when
     saving a custom CPU model config, making the default visible in the config
     file.
 
