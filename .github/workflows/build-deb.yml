name: Build debian package for submodule

on:
  workflow_dispatch:
    inputs:
      package:
        description: "The package to build"
        required: true
        default: "pve-manager"
        type: string
      version_increments:
        description: "Patch level"
        required: false
        default: "1"
        type: string
  workflow_call:
    inputs:
      package:
        description: "The package to build"
        required: true
        default: "pve-manager"
        type: string
      version_increments:
        description: "Patch level"
        required: false
        default: "1"
        type: string

permissions:
  # for ghcr.io (docker registry):
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.WORKFLOW_TOKEN }}

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        env:
          VERSION_INCREMENTS: ${{ inputs.version_increments }}
        run: |
          sed -i -e 's|git@github.com:|https://github.com/|' .gitmodules # switch from ssh to https because we don't have ssh keys in the pipeline
          git submodule update --init submodules/${{ inputs.package }}

          #pushd submodules/${{ inputs.package }}
          #patch -p1 -i ../${{ inputs.package }}.changelog.patch
          #popd

          GIT_CHANGES=$(git log -1 --pretty=format:%s -- submodules/${{ inputs.package }}.patch)
          docker run --rm -v .:/src -w /src/submodules/${{ inputs.package }} -e EMAIL=docker@wsh.no -t ghcr.io/hwinther/wsh-pve/pve-build:12 dch -l +wsh -D bookworm "$GIT_CHANGES"
          grep "\+wsh" submodules/${{ inputs.package }}/debian/changelog

          tag_latest=ghcr.io/hwinther/wsh-pve/${{ inputs.package }}:latest
          docker build . -f submodules/${{ inputs.package }}.Dockerfile -t $tag_latest
          docker push $tag_latest

          git config user.name "${{ github.actor }}"
          git config user.email "${{ github.actor }}@users.noreply.github.com"
          git diff -p submodules/${{ inputs.package }}/debian/changelog > submodules/${{ inputs.package }}.changelog.patch
          git add submodules/${{ inputs.package }}.changelog.patch
          git commit -m "Update changelog for ${{ inputs.package }}"
          # git push origin

          # test/list content of image:
          # id=$(docker create $tag_latest)
          # docker cp $id:/opt/repo/ ./build/
          # docker rm -v $id
          # ls -la build
